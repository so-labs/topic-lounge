# AI/開発者向け指示書

このファイルには、本プロジェクト（Topic Lounge）に機能やツールを追加する際、AIエージェントまたは人間の開発者が厳格に遵守すべきUI、アーキテクチャ、実装ルールを定義しています。

---

## 🎨 1. UI・スタイルルール

- **カラーシステム**:
  - メインカラーはオレンジ（`--primary-color: #F39C12`）ベースです。
  - `style.css` に定義されたCSS変数を利用し、ライト・ダーク・システムテーマに追従するようにしてください。

---

## 🏗 2. アーキテクチャとAPI通信ルール

- **サーバーレス関数（Vercel Functions）**:
  - Gemini APIの呼び出しはフロントエンドから直接行わず、必ず `api/generate.js` や `api/relay.js` などのサーバーレス関数を経由してください。
  - フロントエンドからAPIキーを直接参照することは厳禁です。
- **モデルの管理 (`models.json`)**:
  - 利用可能なGeminiモデルは `models.json` で管理されています。
  - リレー小説機能など、応答速度が重視される機能では `lightweight: true` が設定されたモデルのみを使用許可とするルールを遵守してください。
- **エラーハンドリング**:
  - UIでのエラー表示は `js/errorDisplay.js` の `showError` などの共通モジュールを使用してください。

---

## 📦 3. リリース・バージョン管理ルール

- **キャッシュのバージョン管理 (`sw.js`)**:
  - Service Workerで使用するキャッシュ名 (`CACHE_NAME`) は、以下の規則で運用してください。
  - フォーマット: `topic-lounge-YYYY.MM-rN` (例: `topic-lounge-2026.09-r4`)
  - 変更時は必ず `sw.js` 内のこのバージョン文字列を更新してください。

---

## 📝 4. コミットルール

AIエージェントおよび人間の開発者がコミットを作成する際は、必ず以下のルールに従ってください。

### 1) 手順

1. `git status` で変更状況を確認する。
2. 過去のコミットを10件取得してスタイルを確認する（`git log -10` などを使用し、`--oneline` は使用しないこと）。
3. 変更内容（diff）を確認し、必要に応じてテストを実行する。
4. 下記のスタイルに従ってコミットする。
5. **絶対に `git push` は実行しないこと。**

### 2) コミットメッセージのスタイル

```plaintext
feat: ここに大まかな内容

ここに、具体的な説明。

- `ファイル名`: 詳細な単一の説明。
- `ファイル名2`:
  - 詳細な複数の説明1。
  - 詳細な複数の説明2。
```

- `feat` の部分は変更内容に応じて Conventional Commits に従うこと。
- ファイル名はバッククォート（\`）で囲むこと。
- 具体的な説明は単一行にし、詳細な説明が複数ある場合は箇条書きで行を分ける。箇条書きの間に不要な空行は入れないこと。
- 変更内容の説明は硬い説明調を避け、「〜しました」「〜対応しました」「〜に変更しました」など、自然な完了報告の表現にすること。
